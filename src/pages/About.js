import Layout from "../components/Layout";

export default function ContactDelete()
{
    return(
        <Layout>
            <h2 className="display-4">About Page</h2>

            <p>
                This web app is designed to interact with a back end via the 
                MERN stack in order to nicely and cleanly display a list of
                contacts that can be added to and removed from.
            </p>
        </Layout> 
    )
}